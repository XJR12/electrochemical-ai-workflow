# 260812 周三

一句话总结：为电化学数据处理新增 Tafel 模块，用 260812 真实数据跑通“LSV 选线 → lsv_best.csv → Tafel 出图”完整链路，并补齐 README 前置步骤。

（本记录覆盖 8 月 12 日整日，含 8 月 13 日凌晨 4 点前的连续工作。）

## 今日主线

- 主线目标：在电化学数据处理里加入 Tafel 模块，计算 `log10(j)-η` 坐标并出论文级图
- 背景/起因：现有模块全部是 LSV 处理；用户需要 Tafel 图，坐标约定为横轴 `log10(j)`、纵轴 `η(mV)`，只使用 iR 后数据，v1 不拟合斜率
- 进展：
  - 新增 `电化学数据处理/tafel_plot.py`：`--files` 直接指定 iR txt 出图，`--best-csv` 读取 LSV 已选结果出图；每张曲线同时输出全数据版和截取版（默认 `j ≥ 1 mA·cm⁻²`）
  - 坐标计算：`j = I / area`（默认 1.0 cm²），`η = (E − 1.23) × 1000` mV
  - `config.yaml` 增加 `tafel:` 配置段；README 补充 Tafel 用法
  - 用户纠错确认：`lsv_best.csv` 必须由 LSV 流程生成，不能造临时夹具代替；README 已补“先跑 `lsv_pipeline.py`”的前置步骤
  - 用 260812 实测：`lsv_pipeline.py` 首次出现手动候选，按“η10 更低优先”暂选三组后重跑完成
  - 确认“只选线不跑 LSV 图”：`lsv_pipeline.py --skip-plot --skip-compare`，实测 `260812_lsv_select_only → 260812_tafel_select_only`
- 产出：
  - 新建：`电化学数据处理/tafel_plot.py`、`长期记忆/每日总结/260812.md`
  - 修改：`电化学数据处理/config.yaml`、`README.md`
  - 输出：`电化学数据处理/output/260812_lsv`、`260812_tafel`、`260812_lsv_select_only`、`260812_tafel_select_only`、`260812_tafel_direct`
  - 临时依赖：`临时文件/tafel_deps`（matplotlib、pyyaml）

## 今日支线与灵感

- 主题：`--rhe` 参数含义
  - 背景/起因：用户看到 Tafel 参数 `--rhe` 提问
  - 讨论要点：RHE 基准电位默认 1.23 V；`η = (E − 1.23) × 1000`；以后换参比或校正可改参数不改代码
  - 现状/结论：已按此写入 README
- 主题：Tafel 是否要斜率拟合
  - 背景/起因：用户对斜率拟合不熟
  - 讨论要点：先出全数据版和截取版看效果，v1 不做斜率拟合
  - 现状/结论：已按此实现，后续可再加

## 今日关键决定

- Tafel 坐标：`x = log10(j)`，`j = I / 面积`；`y = η = (E − 1.23) × 1000` mV
- Tafel 只用 iR 后数据，raw 文件跳过并警告
- `lsv_best.csv` 只能来自 LSV 流程（`lsv_analysis.py` 写出），不能用临时文件代替
- 只想选线时用 `lsv_pipeline.py --skip-plot --skip-compare`
- 260812 手动候选暂按 η10 更低优先选：`3%-1g-0802=4-LSV_C01.txt`、`4.5%-1mg-0807=IR-2_02_LSV_C01.txt`、`Ru-1mg-0807=1-LSV_C01.txt`

## 待完成事务

- 用户确认 260812 三组手动选线是否采用上面的暂选方案
  - 为什么重要：Tafel 对比图基于 LSV 选出的 iR 曲线
  - 下一步动作：若需调整，告诉我要选的文件后重跑 LSV 再出 Tafel
- 用户抽查 Tafel 图并确认截取范围（默认 `j ≥ 1 mA·cm⁻²`）是否合适
  - 为什么重要：影响截取版图的可读性
  - 下一步动作：查看 `output/260812_tafel` 后调整 `config.yaml` 的 `tafel.j_min`
- 每日总结自动提醒仍未配置（沿用 260807 遗留）

## 待确认问题

- 260812 三个手动组的最终选线是否采用暂选方案
- 截取版默认 `j ≥ 1` 是否合适；是否后续要加 Tafel 斜率拟合

## 明日建议

- 先让用户确认 260812 选线和 Tafel 图效果；确认后把 Tafel 接到正式输出目录，再考虑斜率拟合

## 文件与路径变动

- 新建：`电化学数据处理/tafel_plot.py`、`长期记忆/每日总结/260812.md`
- 修改：`电化学数据处理/config.yaml`、`README.md`
- 输出：`电化学数据处理/output/260812_lsv`、`260812_tafel`、`260812_lsv_select_only`、`260812_tafel_select_only`、`260812_tafel_direct`
- 临时：`临时文件/tafel_deps`
- 未提交 git；`RuO2/测性能/260812` 为用户新数据，未改动
