# 260804 周二

一句话总结：按留档思路完成 LSV 对比图、统一选线规则，并把过电位检测与画图合并成一条命令，准备首次 git 上传。

（本记录覆盖 8 月 4 日凌晨 4 点后至收工的工作；4 点前的部分见 260803.md。）

## 今日主线

- 主线目标：落实 260804 LSV 自动化绘图思路总结（留档版），完成对比图模块、统一选线规则，并把“检测+画图”整合成一条命令
- 背景/起因：单样品图已可用；需要多条件对比图；η10/η100 选线规则需要统一；最终希望一条命令完成 分析→画图→对比
- 进展：
  - 将 260804 LSV 自动化绘图思路总结（留档版）存档到 长期记忆/，核心思想：固定代码 + 配置，AI 不自由设计；单位明确 mA → mA·cm⁻²
  - 新增 lsv_compare.py：总对比 LSV/IR、按目录名空格拆词自动分组、极差样品自动剔除、y 轴 0-100/0-10 自适应、x 轴 1.4 起自动收；260803 数据生成 10 张对比图 + 选择说明
  - 统一选线规则：η10 差距 ≤5mV 的候选里优先选 η100 最低，差距大标记“手动”；同步进 lsv_analysis.py
  - 更新汇报文档：slow 1mg 按新规则改为 2-LSV 的 263/494
  - 合并：主目录 电化学数据处理/ 新增 lsv_pipeline.py 一键跑 分析→样品图→对比图；LSV/ 暂存备份保留
  - git 准备工作：仓库已初始化在 main、无提交；确认需补远程地址、提交身份、提交范围
- 产出：长期记忆/260804 LSV 自动化绘图思路总结（留档版）.md、电化学数据处理/lsv_compare.py、lsv_pipeline.py、config.yaml、output/（2 CSV + 18 样品图 + 10 对比图 + 选择说明）、RuO2/汇报/2026-08-03-汇报总结.md 更新

## 留档版思路的落地情况

- 已落地：
  - 固定代码 + config.yaml 分离“怎么画”和“画成什么样”，AI 只维护代码、不自由改图
  - 单位规范：输入 mA、电极面积默认 1 cm²、纵轴写 Current density (mA·cm⁻²)
  - 模块结构：lsv_analysis.py / lsv_plot.py / config.yaml / data / output，另新增 lsv_compare.py
  - 绘图规范：Arial、600 dpi、线宽 2、无 marker、x 轴 1.4 V 起、大分度 0.05 / 小分度 0.025、坐标标题加粗
  - 工作流：复制 txt → 一条命令 → η10/η100 CSV → 样品图 → 对比图
- 按讨论调整或未落地：
  - 原计划 η10.xlsx / η100.xlsx 改为中文 CSV（用户认为更顺手）
  - iR 修正由工作站完成，脚本只读取 iR 文件
  - 对比图、自动分组、极差剔除为新增内容
  - 打包成 electrochemical LSV plotting skill 尚未做

## 今日支线与灵感

- 主题：对比图自动分组能否代码化
  - 背景/起因：不希望每次手动写“1mg”“low”这类组名
  - 讨论要点：目录名按空格拆词，取至少 2 个目录共有的词自动成组；贯穿所有目录的恒定词（如都是 3%）跳过；特殊跨词对照留 config.manual_groups
  - 现状/结论：已实现，下次命名保持“空格分词”格式即可自动出组
- 主题：选线规则统一
  - 背景/起因：之前按最低 η10，用户希望 η10 相近时优先 η100
  - 讨论要点：5mV 并列阈值 + η100 优先；差距大时列候选人工
  - 现状/结论：已同步到分析脚本，slow 1mg 代表曲线改为 2-LSV
- 主题：单位与坐标规范
  - 背景/起因：留档版指出 Origin 图单位标注问题
  - 讨论要点：输入 mA、面积 1cm²、输出 mA·cm⁻²；x 大分度 0.05、小分度 0.025；坐标标题加粗
  - 现状/结论：已落实到 config.yaml 与绘图脚本
- 主题：首次 git 上传
  - 背景/起因：用户第一次用 git，之前是拖拽上传
  - 讨论要点：需要远程地址、提交身份、提交范围；大文件建议不入库
  - 现状/结论：待用户提供信息后执行

## 今日关键决定

- 对比图选线：η10 ≤5mV 并列时取 η100 最低；差距大标记手动
- 极差样品自动剔除（η10 比组内中位数高 100mV 以上）
- 分析+画图+对比合并为 lsv_pipeline.py 一条命令
- LSV/ 暂存备份保留，确认新入口没问题后删除
- git 首次提交建议忽略 临时文件/、__pycache__、生成物；RuO2 大文件暂不入库

## 待完成事务

- git 首次上传：建 .gitignore（__pycache__、*.pyc、临时文件/、生成物等）、git add/commit、push；等用户提供远程地址和身份
- 删除 电化学数据处理/LSV/ 暂存备份（用户确认后）
- 汇报文档其余组数值是否按脚本最优更新
- 下一轮合成：纯 Ru、1.5%Ce、4.5%Ce（配比已算好）
- 每日总结自动提醒时间未配置

## 待确认问题

- git 远程仓库地址、user.name/user.email、提交范围（建议忽略大文件）
- 对比图 1mg 组中旧批次曲线是否保留、y/x 范围是否再调
- 汇报文档其余组数值更新范围
- LSV/ 备份是否删除

## 明日建议

- 完成 git 首次提交上传，把当前代码版本固化；随后开始下一轮 纯Ru/1.5%/4.5% 的实验记录，或按用户反馈继续打磨绘图细节

## 文件与路径变动

- 归档后删除：长期记忆/260804 LSV 自动化绘图思路总结（留档版）.md（已提炼进本篇）
- 新建：电化学数据处理/lsv_compare.py、lsv_pipeline.py、config.yaml、output/
- 更新：电化学数据处理/lsv_analysis.py、lsv_plot.py、README.md、RuO2/汇报/2026-08-03-汇报总结.md
- 保留：电化学数据处理/LSV/（合并前备份）